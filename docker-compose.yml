services:

  squeak:
    build:
      context: .
      dockerfile: docker/Dockerfile.squeak
    container_name: kethernet-squeak
    environment:
      - DISPLAY=${DISPLAY:-:0}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ./squeak:/squeak:ro
      - navi-data:/navi:rw
    stdin_open: true
    tty: true
    networks:
      - kethernet

  pharo:
    build:
      context: .
      dockerfile: docker/Dockerfile.pharo
    container_name: kethernet-pharo
    stdin_open: true
    tty: true
    volumes:
      - ./pharo:/pharo:ro
      - navi-data:/navi:rw
    networks:
      - kethernet

networks:
  kethernet:
    driver: bridge

volumes:
  navi-data: